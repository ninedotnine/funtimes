make > and < function in if statements

make disable = pause

make disable = save

make disable = action (to make a room that does not require any action - can be used to set variables, or for a cutscene)

improve error messages for if

optimize BadPredicamentError

allow index in if and set statements (for items, quests)
